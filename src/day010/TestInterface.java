package day010;

public interface TestInterface {
    // * 멤버변수 불가능
    //int a;
    public static final int b = 10; // 상수는 가능. (호출도 가능)
    // * 생성자 불가능
    // * 추상메소드: 메소드 선언만 한다. (선언부)만 있고 {구현부}가 없다. -> 그럼 구현은 누가해요 -> 구현체(Tool)가 해요
    //   추상메소드는 오버라이딩 당함...
    // 상속과 비슷하지만, 물려줄 때 껍데기만 주고 (구현 안 하고) 만다.
    // 👇 껍데기
    public abstract void method1(); // abstract 는 자동으로 들어간다. public도 자동인데 왜 넣어요 -> 그냥 넣으라고 하셨어요
    void method2( int x ); // public abstract는 생략이 가능하다.
}
